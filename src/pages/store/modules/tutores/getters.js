export default {
  // tutores(state) {
  //   return state.tutores;
  // },
  // hasTutores(state) {
  //   return state.tutores && state.tutores.length > 0;
  // },
  // isCoach(_, state, _2, getters, rootState, rootGetters) {
  //   const tutores = getters.tutores;
  //   const userId = rootGetters.userId;
  //   return tutores.some((tutor) => tutor.id === userId);
  // },
};
